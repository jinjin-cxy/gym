<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinjin.gymmanagementsystem.mapper.MemberMapper">
    <select id="selectMemberCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM member
    </select>

    <select id="selectMemberByMemberAccount" parameterType="java.lang.Integer" resultType="com.jinjin.gymmanagementsystem.pojo.Member">
        SELECT * FROM member WHERE member_account = #{memberAccount}
    </select>

    <select id="findAll" resultType="com.jinjin.gymmanagementsystem.pojo.Member">
        SELECT * FROM member
    </select>

    <insert id="insertMember" parameterType="com.jinjin.gymmanagementsystem.pojo.Member">
        INSERT INTO member (member_account, member_password, member_name, member_gender,
        member_age, member_height, member_weight, member_phone, card_time, card_class, card_next_class)
        VALUES (#{memberAccount}, #{memberPassword}, #{memberName}, #{memberGender},
        #{memberAge}, #{memberHeight}, #{memberWeight}, #{memberPhone}, #{cardTime}, #{cardClass}, #{cardNextClass})
    </insert>

    <delete id="deleteMemberByMemberAccount" parameterType="java.lang.Integer">
        DELETE FROM member WHERE member_account = #{memberAccount}
    </delete>

    <update id="updateMemberByMemberAccount" parameterType="com.jinjin.gymmanagementsystem.pojo.Member">
        UPDATE member
        SET member_password = #{memberPassword},
        member_name = #{memberName},
        member_gender = #{memberGender},
        member_age = #{memberAge},
        member_height = #{memberHeight},
        member_weight = #{memberWeight},
        member_phone = #{memberPhone},
        card_class = #{cardClass},
        card_next_class = #{cardNextClass}
        WHERE member_account = #{memberAccount}
    </update>

    <select id="selectMemberByAccountAndPassword" resultType="com.jinjin.gymmanagementsystem.pojo.Member">
        SELECT * FROM member WHERE member_account = #{memberAccount} AND member_password = #{memberPassword}
    </select>
</mapper>